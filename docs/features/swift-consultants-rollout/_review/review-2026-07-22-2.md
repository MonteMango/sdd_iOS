---
status: Draft
owner: "Vitalii Lytvynov"
reviewers: ["Tech Lead"]
updated_at: "2026-07-22"
feature_size: "L"
---

# Review — swift-consultants-rollout — 2026-07-22 (follow-up)

## Scope

Follow-up re-review of the fix commits addressing `review-2026-07-22.md`'s CHANGES REQUESTED
verdict: `e0d5364..HEAD` — `16748bb` (stage-2 doc findings #6-8), `8550395` (commit the 4
`sdd.local.md` fixture variants — finding #5), `05c1314` (close findings #1, #2, #4 with real
fixture evidence). 13 files changed, 199 insertions(+) / 24 deletions(-) across
`evals/scenarios/{implement,plan-tests,review,sequences}-ios-consultant/`,
`skills/{plan-tests,sequences}/SKILL.md`, `skills/_shared/consultant-{trigger,fold}.md`, and
`docs/features/swift-consultants-rollout/sad.md`.

Per this skill's anti-pattern guidance ("re-review the changed surface" after CHANGES REQUESTED,
not the whole feature diff from scratch), scope is the fix commits, not the full
`2b75541..HEAD` feature diff already covered by the first review's 4-reviewer fan-out.

## Method

2 independent `sdd:reviewer` subagents, clean-context, dispatched in parallel:

1. **Substantive fixture fixes** — verify findings 1 (AC-10 denial-path), 2 (AC-07 3-of-3
   citation), 4 (implement fallback marker), 5 (sdd.local.md variants committed) are genuinely
   closed, not just claimed closed.
2. **Stage-2 doc fixes** — verify findings 6 (explicit dispatch string), 7
   (`swift-testing-expert` → `swift-testing-consultant` rename), 8 (sad.md brownfield repoint),
   plus a general regression scan over the touched files.

## Findings

### All 8 original findings — verified CLOSED

| # | Finding | Verdict | Evidence |
|---|---|---|---|
| 1 | AC-10 denial-path (plan-tests + sequences) | **CLOSED** | Both stages now inject a real code-altitude item into the simulated brief (a concrete `XCTAssertThrowsError`/XCTest mention for plan-tests, a `Task.sleep`-based retry-loop detail for sequences) and show the fold denying it — before/after content shown, not asserted. |
| 2 | AC-07 3-of-3 citation (review) | **CLOSED** | Updated record cites all 3 consultants at equal weight, including swift-testing-consultant's finding at `ProfileSyncTests.swift:4-7` — confirmed the citation matches the real fixture file content. |
| 3 | spec.md never committed | **CLOSED** (fixed during the first review, `9f219a1`) | No change since. |
| 4 | implement fallback marker unexercised | **CLOSED** | New `fallback-marker` run shows the marker at both required placements (task commit body + final handoff) with wording matching `consultant-fold.md`'s template. |
| 5 | sdd.local.md variants uncommitted | **CLOSED** | All 4 variants committed under `evals/scenarios/implement-ios-consultant/sdd-local-variants/`, content genuinely routing-relevant, linked from the README. |
| 6 | plan-tests/sequences dispatch missing explicit `subagent_type` + fallback | **CLOSED** | Both `SKILL.md`s now match `design/SKILL.md`'s precedent shape exactly. |
| 7 | shared template names `swift-testing-expert` instead of shipped `swift-testing-consultant` | **CLOSED** at the 3 originally-cited locations | See new finding below — the rename didn't fully propagate past those 3 lines. |
| 8 | sad.md brownfield note pointed at a deleted path | **CLOSED** | Repointed to `skills/_shared/...`; old path confirmed gone. |

### New findings from this re-review — resolved during this pass

| # | Finding | file:line | Resolution |
|---|---|---|---|
| A | Finding 7's rename was surgical to the 3 originally-cited lines only; 5 more locations still named the consultant `swift-testing-expert`, re-introducing the exact drift finding 7 existed to remove (independently flagged by both fan-out reviewers). | `evals/scenarios/review-ios-consultant/README.md:48`, `docs/features/swift-consultants-rollout/sad.md:18,110,112,171,278` | **Fixed now**, applied directly in this review pass (same precedent as finding 3 in the first review record). All 6 locations renamed to `swift-testing-consultant`; `sad.md:41` (the third-party *bundle* name, a distinct entity) correctly left unchanged. |
| B | New `fallback-marker` row tagged AC-02/AC-03; the finding it closes (finding 4) and `SKILL.md:71` both tie the implement marker to AC-05/AC-06. | `evals/scenarios/implement-ios-consultant/README.md:42` | **Fixed now** — retagged to AC-05, AC-06. |
| C | Same row cited `consultant-fold.md`'s "dual-placement rule" as authority for the commit-body placement, but `consultant-fold.md:47` calls the single-agent placement an "inline-consult note"; the actual commit-body authority is `tdd-loop.md:27`. | `evals/scenarios/implement-ios-consultant/README.md:42` | **Fixed now** — split the citation: `tdd-loop.md:27` for the commit-body placement, `consultant-fold.md` for the marker format + handoff requirement. |

No stage-1 (AC-compliance) findings remained open after this pass — findings A/B/C are all
stage-2 doc-accuracy issues, resolved directly (doc-only, no TDD loop needed).

## Gate result

**Verdict: PASS.**

All 8 findings from `review-2026-07-22.md` are independently confirmed closed with real evidence
(not just claimed). 3 new stage-2 doc-accuracy findings surfaced during the re-review — all
fixed directly in this pass, no code or test changes involved, no open finding remains.
