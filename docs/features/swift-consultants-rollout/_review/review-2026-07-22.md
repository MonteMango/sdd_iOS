---
status: Draft
owner: "Vitalii Lytvynov"
reviewers: ["Tech Lead"]
updated_at: "2026-07-22"
feature_size: "L"
---

# Review — swift-consultants-rollout — 2026-07-22

## Scope

Committed feature diff, `2b75541..HEAD` (base = the `design-swift-consultants` merge this rollout
builds on): 60 files changed, 2687 insertions(+) / 98 deletions(-) across `agents/*-consultant.md`,
`skills/{implement,plan-tests,review,sequences,design}/`, `skills/_shared/consultant-{trigger,fold}.md`,
`scripts/validate_plugin.py`, `docs/features/swift-consultants-rollout/{sad.md,adr/,tasks.json,tasks/}`,
and `evals/scenarios/{implement,plan-tests,review,sequences}-ios-consultant/`.

Claimed ACs (`SDD-AC` trailers across the 16 implementation commits): AC-01 .. AC-10, AC-10b —
the full spec §5 set.

Feature size **L**, route **full** — per policy this fans out one reviewer per dimension rather
than a single pass.

## Method

4 independent `sdd:reviewer` subagents, each clean-context (re-read spec/sad/ADRs/tasks.json
itself, no paraphrase), dispatched in parallel:

1. **implement-stage wiring** (AC-02, AC-05, AC-06)
2. **plan-tests + sequences wiring** (AC-01, AC-08, AC-09, AC-10) — plus the `_shared/`
   consultant-trigger/fold relocation (ADR-0004)
3. **review-stage wiring** (AC-03, AC-05, AC-07, AC-09, AC-10b)
4. **cross-cutting**: AC-04 plugin-validation gate invariant + the 3 agent files, plus the
   whole-feature §4 user-story / §5 AC backstop trace (spec → sad.md §6 flow → tasks.json →
   implement prose/code → eval fixture), per this skill's end-to-end trace dimension

## Findings

### Stage 1 — blocking (AC compliance)

| # | Finding | file:line | AC | Resolution |
|---|---|---|---|---|
| 1 | Both `plan-tests`/`sequences` fixtures prove AC-10's denial rule only by absence — the consultant's real reply never contained a code-altitude item, so nothing was ever actually denied by the altitude filter. No fixture injects a code-altitude item into the brief and confirms the fold denies it. Tautological ("nothing to deny → nothing denied"), not proof the guard works. | `evals/scenarios/plan-tests-ios-consultant/README.md:55-57`, `evals/scenarios/sequences-ios-consultant/README.md:50-52` | AC-10 | **Fix now** → follow-up task for `implement`: add one injected-brief run per stage forcing a code-altitude item (concrete tool/framework name for plan-tests; a retry-loop implementation for sequences) into the returned brief, and record that the fold denied it. |
| 2 | AC-07 requires all 3 signalled consultants to fire with a cited, equally-weighted finding. The fixture's own record cites findings from only 2 of 3 (concurrency-, swiftui-consultant); swift-testing-consultant's real, catchable finding (`ProfileSyncTests.swift:4-7` — the test `await`s an async call but asserts nothing via `#expect`) is never enumerated. Practical evidence today is 2/3, not 3/3. | `evals/scenarios/review-ios-consultant/README.md:46`, `evals/scenarios/review-ios-consultant/diff-files/ProfileSyncTests.swift:4-7` | AC-07 | **Fix now** → follow-up task for `implement`: cite the swift-testing-consultant's finding in the review-record evidence cell, proving 3/3 at equal blocking weight. |
| 3 | `spec.md` — the source of truth every AC trace in this review depends on — was never committed to git, while every downstream artifact (sad.md, 5 ADRs, tasks.json, 16 implementation commits) is already committed and was built referencing it. Flagged independently by 2 of 4 fan-out reviewers. | `docs/features/swift-consultants-rollout/spec.md` (file-level) | all | **Fixed during this review.** Committed in `9f219a1` (spec.md + `.size` + `.route`). |

### Stage 2 — quality, non-blocking

| # | Finding | file:line | AC | Resolution |
|---|---|---|---|---|
| 4 | `implement`'s fallback marker (consultant unavailable) is fully specified across all 3 modes (SKILL.md, tdd-loop.md, team-exec.md, workflow-exec.md) but no fixture exercises it — unlike `design`'s precedent, which ships a dedicated `bundle-unavailable-spec.md`. | `evals/scenarios/implement-ios-consultant/` (missing fixture) | AC-05/AC-06 | **Fix now** → follow-up task: add an `implement` bundle-unavailable fixture run proving the per-task marker lands in the TaskList body / `consultant_brief` / inline note, and in the handoff's *What I did*, for at least one signalled task. |
| 5 | The `implement` fixture's README names 4 `.claude/sdd.local.md` variants (single/team/workflow/conflict) that drive every result, but none is committed — AC-06's headline evidence (project settings beat consultant advice) rests on an unsaved file and un-rerunnable live output. | `evals/scenarios/implement-ios-consultant/README.md:12` | AC-02/AC-06 | **Fix now** → follow-up task: commit the 4 `.claude/sdd.local.md` variants (or a documented exact snippet) alongside the fixture. |
| 6 | `plan-tests` and `sequences` dispatch steps give only a markdown link to the consultant's agent file, without the explicit `subagent_type: "..."` + `fallback general-purpose` line `design`'s precedent uses. | `skills/plan-tests/SKILL.md:42`, `skills/sequences/SKILL.md:45` | AC-01/AC-08 | **Fix now** → follow-up task: add the explicit dispatch string + fallback to both steps. |
| 7 | The shared fallback-marker template names the class `swift-testing-expert`, but the shipped agent (and the fixture's own marker) is `swift-testing-consultant` — exactly the drift the shared template exists to prevent. | `skills/_shared/consultant-fold.md:43`, `skills/_shared/consultant-trigger.md:13,37` | AC-01/AC-02 | **Fix now** → follow-up task: rename the template's third option to `swift-testing-consultant`. |
| 8 | This feature's own `sad.md` brownfield note still cites `skills/design/references/consultant-{trigger,fold}.md`, which this same feature deleted (relocated to `_shared/` per ADR-0004). | `docs/features/swift-consultants-rollout/sad.md:64` | n/a | **Fix now** → follow-up task: repoint to `skills/_shared/...` (or note "relocated by ADR-0004"). |

### Informational — reviewer-assessed, no action needed

- `implement` team/workflow AC-02/AC-05 has no live nested-harness execution proof (`TeamCreate`/`Workflow` degraded to sequential in the eval harness); disclosed limitation, and the captured-script proof + single-agent/review live-execution paths were judged sufficient coverage by the reviewer. No action.
- T13/T16 DoD text says "3 fixture runs"; 2 invocations were actually used (signal + no-signal folded into one dual-AC spec). Behavior coverage is intact — wording-only mismatch. No action.
- `design/SKILL.md`'s AC citations (e.g. "AC-06" for its own no-op case) belong to the sibling `design-swift-consultants` spec's own AC numbering, not this rollout's — pre-existing, not introduced by this diff, and correct within its own governing feature. Flagged only because both features' AC numbering now coexists in one repo. No action.

## Gate result

**Verdict: CHANGES REQUESTED.**

3 stage-1 findings: 1 fixed directly during this review (spec.md committed), 2 handed back to
`implement` as follow-up tasks (AC-10 denial-path fixture, AC-07 3-of-3 citation). 4 stage-2
findings, all resolved **Fix now** by the feature owner and handed back to `implement` alongside
the stage-1 pair. No stage-1 finding ships unresolved — the two remaining ones are explicitly
queued for the next `implement` pass, not silently dropped.
