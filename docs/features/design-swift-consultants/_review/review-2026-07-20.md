---
status: Draft
owner: "Vitalii Lytvynov"
reviewers: ["Tech Lead"]
updated_at: "2026-07-20"
feature_size: "S"
---

# Review — design-swift-consultants — 2026-07-20

## Scope

Committed feature diff (8 commits, `0160ab4..HEAD`): 374 insertions / 10 deletions across
`skills/design/SKILL.md`, `skills/design/references/consultant-trigger.md`,
`skills/design/references/consultant-fold.md`, `docs/features/design-swift-consultants/tasks/tracker.md`,
and `evals/scenarios/design-ios-consultant/{README.md, ui-async-spec.md, pure-logic-spec.md,
bundle-unavailable-spec.md}`.

Reviewer: `sdd:reviewer` subagent, clean context, dispatched with the full upstream artifact set
(spec.md, sad.md, ADR-0001..0004, tasks.json, `blast-radius.md`) plus a direct run of
`scripts/validate_plugin.py`.

## Findings

All 6 findings were cited (`file:line`) and independently re-verified before resolution (grep for
the literal `UI` occurrences in `pure-logic-spec.md`; read of `validate_plugin.py`'s agent-check
logic; read of `t7-ac04-roster-invariant.md`'s frontmatter/DoD state; read of `blast-radius.md`'s
scoring semantics).

| # | Stage | Finding | Verdict | Resolution |
|---|---|---|---|---|
| 1 | 1 | `consultant-trigger.md:9,11` — UI-class keyword rule was a raw substring match on `UI`; `pure-logic-spec.md` contains the literal word "UI" 4× (always negated: "no UI dependency" etc.), so the rule as written would have affirmatively fired, contradicting the recorded AC-06 PASS. | CONFIRMED | **Fixed.** Rule tightened to whole-word match + negation-exclusion in `consultant-trigger.md`; `evals/scenarios/design-ios-consultant/README.md` annotated with the correction. |
| 2 | 1/2 | `SKILL.md:79` + ADR-0003 overclaimed that placing the consultant under `agents/*.md` (a stray file, not in the frontmatter list) trips `validate_plugin.py` — the gate only checks the forward direction (`agents:` list → file exists), no reverse orphan-file check. | CONFIRMED | **Fixed.** `SKILL.md:79` reworded to scope the claim to the `agents:`-frontmatter path only, with an explicit note that a stray file isn't independently caught. |
| 3 | 1 | T7's DoD requires a negative check (break the roster invariant, confirm the gate fails, revert) — `tracker.md` marked T7 `done`, but `t7-ac04-roster-invariant.md` frontmatter was still `status: "todo"` with all 3 DoD boxes unchecked; no evidence of the negative check existed anywhere. | CONFIRMED | **Fixed.** Negative check actually performed during this review: baseline gate green (359 checks) → `agents:` temporarily set to `[explorer, critic, consultant]` → gate failed (`FAIL skill 'design' references agent 'consultant' with no agents/consultant.md`, exit 1, 360 checks) → reverted → gate green again (359 checks), clean `git diff`. Evidence + status/DoD recorded in `t7-ac04-roster-invariant.md`. |
| 4 | 1 | `consultant-fold.md:9-12` reused `blast-radius.md`'s 0-of-3/2-of-3 ADR-vs-inline scoring as if it were an entry-vs-no-entry admission test — but blast-radius.md's 0-of-3 means "write inline into sad.md," not "deny entry." The 1-of-3 borderline disposition was also undefined for the fold context. | CONFIRMED | **Fixed.** `consultant-fold.md` rewritten to define its own admission test (structural / code-level / borderline-defaults-to-denied), explicit about how it differs from blast-radius.md's later, narrower ADR-vs-inline question. |
| 5 | 2 | AC-05 (project-rules-win) has no fixture exercising an actual rule conflict — all 3 T8 runs used scratch workdirs with no `CLAUDE.md`/rules file, so only the "no rules file → generic stands" branch of `consultant-fold.md` was ever run. | CONFIRMED | **Deferred.** Recorded in `spec.md` §8 Open questions — owner: Fork maintainer, due: before Tier-2 rollout. |
| 6 | 2 | `SKILL.md:48` / `consultant-trigger.md:3` said "zero added cost" / "zero cost" on the no-op path, but step 3.5 always reads `consultant-trigger.md` and runs the classification — a nonzero (if tiny) reasoning cost independent of AC-06's actual scope (consultant/bundle token cost). | CONFIRMED (low severity) | **Fixed.** Reworded to "no consultant/bundle token cost" in both files. |

## Gate result

**CHANGES REQUESTED → addressed in this same pass (no `/clear`).** 5 of 6 findings fixed directly
(doc/reference edits + a real negative-check run, no code surface exists for this feature beyond
markdown protocol prose); 1 deferred to spec §8 with owner + due, per the review protocol's Defer
path. `scripts/validate_plugin.py` re-verified green (359 checks) after every edit, including
immediately after the temporary roster-violation revert.

**Verdict: PASS** (post-fix). No open stage-1 (AC-blocking) finding remains unresolved — the one
surviving stage-2 gap (AC-05 fixture coverage) is explicitly deferred, not silently dropped.

## Uncommitted, out-of-scope changes noticed in the working tree

`.claude-plugin/marketplace.json` (`"name": "sdd"` → `"sdd-ios"`) and `server/bun.lock` are dirty in
the working tree but are **not part of any of this feature's 8 commits** and were not touched by
this review. Flagged for awareness only — not attributed to this feature's diff or verdict.
