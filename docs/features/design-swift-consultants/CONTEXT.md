# CONTEXT — design-swift-consultants

Feature-scoped glossary. Readers merge this with the repo-root `CONTEXT.md`; on conflict, this
per-feature entry wins. Canonical for roles + domain terms used in `spec.md`.

## Glossary

<!-- 2026-07-20: every term previously defined here (Pipeline operator, Fork maintainer, Expert
     consultant, Expert skill bundle, Trigger signal, Structural altitude, Blast-radius gate,
     Altitude filter, Observable trace, Fallback marker, Project rules) turned out to be
     cross-feature — swift-consultants-rollout reuses all of them — so per the glossary two-level
     contract (a term lives in exactly one place) they moved to the repo-root CONTEXT.md. No
     definition changed in meaning; Trigger signal's mapping was generalized there to be per-stage
     rather than design-only. Read the root file for all of them; nothing feature-scoped remains
     here. -->
