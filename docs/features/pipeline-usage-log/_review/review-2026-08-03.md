---
feature: pipeline-usage-log
date: "2026-08-03"
reviewer: "sdd:reviewer (clean-context, independent)"
verdict: PASS
---

# Review — pipeline-usage-log — 2026-08-03

## Scope

Diff reviewed: `git diff main..HEAD` (branch `pipeline-usage-log`), 30 files changed, +1619/-10.

- `skills/_shared/pipeline-log.md` — new shared protocol/template (the production artifact).
- `skills/{specify,design,tasks,implement,review,ship,fix}/SKILL.md` — one wiring step each.
- `docs/architecture-map.md` — protocol-count doc note.
- `docs/features/pipeline-usage-log/{spec.md,sad.md,tasks.json,tasks/*,adr/*,CONTEXT.md,.size,.route}`.
- root `CONTEXT.md`.

Out of scope (pre-existing, unrelated working-tree changes, not part of this diff):
`.claude-plugin/marketplace.json`, `server/bun.lock`.

This is a meta-feature: it changes the SDD pipeline's own prose-instruction skill files, not
application code. There is no automated test suite — the feature's entire acceptance evidence is
task T10, a manual walkthrough run on a disposable scratch feature (`scratch-log-walkthrough`,
deleted after), with its pass/fail table recorded in
`docs/features/pipeline-usage-log/tasks/T10-acceptance-walkthrough.md` (committed as `b5c8017`).

## Stage 1 — AC / spec compliance

**No blocking findings.** All 12 AC (AC-01..AC-08 incl. sub-letters) trace end-to-end:
spec §5 → `sad.md` §6 flow → `tasks.json` task → the actual `skills/_shared/pipeline-log.md` rule
+ the corresponding `skills/*/SKILL.md` wiring → a PASS row in T10's evidence table. All 8 user
stories (US-01..US-08) map to ≥1 AC and a §6 flow — none dropped out. The rollup-ownership boundary
(AC-05/AC-05b) was independently confirmed by the reviewer via `grep -i rollup skills/*/SKILL.md`
(only `ship`/`fix` mention it). ADR-0002's mode-aware capture branching matches
`skills/_shared/pipeline-log.md`'s "Mode-aware capture" section verbatim.

**Judgment call — accepted, not a blocker:** T10 leaves one item open — `implement`'s team/workflow
usage-capture path (ADR-0002) was never exercised live, since the scratch feature was XS/1-task and
ran sequential mode only. The reviewer's assessment, which we accept: the worst-case outcome of an
unexercised team/workflow path is the AC-04 "unavailable" marker — a design-sanctioned degradation,
not a false number — and the write/accumulate/label mechanics common to all three modes were
verified live in sequential mode. The gap is honestly flagged (T10 DoD checkbox left unchecked,
sad §11 risk row already names it), not silently marked as verified. Resolution (per user): leave as
recorded in T10 — no additional spec §8 entry.

## Stage 2 — quality

Two non-blocking findings, both fixed in this review pass (commit `e972d56`):

1. **Heading-casing mismatch in tasks.json DoD text** (T2–T8) — the DoD prose named lowercase
   section headings (`### specify`, `### fix`, …) while the shipped protocol
   (`skills/_shared/pipeline-log.md`) and every `SKILL.md` wiring use capitalized headings
   (`### Specify`, `### Fix`, …). Since exact-heading match is case-sensitive, the mismatch was
   text-only (no behavior gap — the wiring in `SKILL.md` is the source of truth and was already
   correct), but left future editors reading `tasks.json` at risk of miscopying the wrong case.
   **Fixed:** casing corrected in `tasks.json` for T2–T8.
2. **T1's `acs` under-declared AC-07** — T1's deliverable (`pipeline-log.md`) already specifies the
   post-ship rollup-refresh rule that AC-07 names, but T1's `acs` array didn't list it (AC-07 was
   only claimed via T8). No real coverage gap — AC-07 was and remains covered — but the trace was
   incomplete. **Fixed:** `AC-07` added to T1's `acs`.

No other stage-2 issues: the 7 `SKILL.md` edits are minimal/surgical (one step each), don't
duplicate the shared template inline, and match their own T2–T8 DoD. `architecture-map.md`'s
protocol-count delta (+1, 13→14) is accurate. No non-goal was violated in the diff (no cross-feature
aggregation, no orchestrator-token capture, no optional-stage logging beyond exclusion-from-rollup,
no live monitoring).

## Verdict

**PASS.**
