# Review record — artifact-language-ru (2026-08-06)

## Scope

Diff reviewed: `93f68b3..4e8460c` (spec through tasks-tracker-complete), 22 files, +944/-3.
Independent reviewer: `sdd:reviewer` subagent, clean context, read-only.

## Stage 1 — AC / user-story trace

**PASS.** US-01→AC-01/02/05, US-02→AC-06, US-03→AC-04/07, US-04→AC-08, US-05→AC-03 — every
user story has ≥1 AC. AC→task trace is complete (AC-01/03→T1,T7; AC-02/04/05→T8; AC-06→T2,T3,T4;
AC-07→T6; AC-08→T5). One caveat surfaced and fixed (see finding 2 below): the AC-01 PASS evidence
depended on an undocumented model override.

## Stage 2 — quality

**CHANGES REQUESTED**, 6 findings, all resolved in this same pass (no `/sdd:implement` iteration
needed — doc/config-only fixes).

| # | Finding | file:line | Resolution |
|---|---|---|---|
| 1 | Russian paragraph inside English `spec.md`, violating this feature's own "artifacts stay English" rule (spec §8 OQ-2, `tasks/_epic.md`) | `spec.md:138` | **Fixed** — paragraph translated to English |
| 2 | AC-01 PASS evidence rests on an undocumented `SDD_EVAL_MODEL=sonnet` override; the verification command recorded in commit `4e8460c` (`--model sonnet`) does not run through `evals/run.sh` (no `--model` flag, env-only) | `rubric.md`, `evals/README.md:57`, `evals/run.sh:15-16` | **Fixed** — run condition documented in `rubric.md` and `evals/README.md`; risk added to `sad.md` §11 |
| 3 | (same as #2 — non-reproducible verification command) | `evals/run.sh:15-16` | **Fixed** — covered by #2's documentation fix |
| 4 | `uk` fixture's template comment drifted from the updated `settings.md` template (`# en \| uk` vs `# en \| uk \| ru`) | `evals/scenarios/glossary-artifact-language-uk/fixture/.claude/sdd.local.md:3` | **Fixed** — comment updated to match |
| 5 | `rubric.md` required a Russian-specific marker in **each** of the two definitions — stricter than AC-01, which only requires the eval to check *a* marker | `evals/scenarios/glossary-artifact-language-ru/rubric.md:12-19` | **Fixed** — relaxed to "at least one of the two definitions carries the marker, and both are Russian prose" |
| 6 | AC-08 not named in `sad.md` §6 Flow 2's N/A block, unlike the other doc-only ACs | `sad.md:166` | **Fixed** — AC-08 added to the N/A rationale |

## What was checked and found clean

- AC-06 discoverability across all 4 named spots (`settings.md` bullet + template, `README.md`,
  `artifact-language.md`).
- AC-07 (repo's own `.claude/sdd.local.md` reads `ru`, stays git-ignored).
- AC-02/AC-04/AC-05 pre-existing guarantees unweakened by T4/T8.
- `evals/README.md` scenario-table completeness (10/15, arithmetic checked against
  `evals/scenarios/` folder count).
- `python3 scripts/validate_plugin.py` — 394/394 checks pass, re-verified after the fix pass.

## Verdict

**PASS** (post-fix). All 6 findings resolved in-pass; no open stage-1 gap remains.

## Next

`/clear`, then `/sdd:ship artifact-language-ru`.
