# Review — workflow-pipeline-unwrap-fix (2026-08-24)

## Scope

- Branch: `workflow-pipeline-unwrap-fix` vs. `main` (merge-base `8e2b958`).
- Diff reviewed: `git diff main...HEAD --stat` at review time —
  `docs/features/workflow-pipeline-unwrap-fix/**` (spec, sad, contracts sync
  report, tasks.json, tracker.md) + the one non-artifact file,
  `skills/implement/references/workflow-exec.md` (+371/-2 across 7 files).
- Implementation commits under review: `483b63d` (fix), `d2b420d` (caveat),
  `20c2bf2` (tracker).
- No API contract change (`contracts/api-sync-report.md` correctly records
  N/A — no runtime interface touched).

## Reviewer

Independent, clean-context dispatch (`sdd:reviewer`, read-only). Traced all
7 ACs (AC-01, AC-02, AC-03, AC-03b, AC-04, AC-04b, AC-05) end-to-end against
the corrected `workflow-exec.md`, both user stories (US-01, US-02), §3
non-goals, and independently re-ran the AC-05 cross-context grep.

## Findings & resolution

| # | Finding | Stage | AC | Resolution |
|---|---|---|---|---|
| 1 | Line 69's added `/ ac_satisfied: false` clause contradicted the code's own comment (line 62: "resolved review, satisfied or not (AC-03b) — never nulled") and AC-03b itself, which requires a resolved-but-unsatisfied review to be **retained**, not dropped to `null`. | 1 (blocking) | AC-03b | **Fixed** — removed the clause, added an explicit "is *not* dropped; it is retained in the results (AC-03b, see the code above)" clarification. Commit `fff9340`. |
| 2 | `if (res == null)` (line 60) checks nullish, while the blockquote/sad.md describe the invariant as "falsy". If a dropped element ever resolved to a non-`null` falsy value, it would slip past the guard as a truthy `{t, res}` entry. | 1 | AC-02 | **Not an issue** — the actual `Workflow` tool contract drops to `null` specifically (not arbitrary falsy); `== null` is correct as written. |
| 3 | The AC-04b "not yet implemented" caveat was appended to the end of one long semicolon-chained bullet, leaving it ambiguous whether it caveats only the skip-cascade clause or the whole bullet (including the live "drops that task to `null`" behavior). | 2 | AC-04b | **Fixed** — split into two sentences: one for the live drop-to-null behavior, one for the skip-cascade clause carrying the caveat. Commit `fff9340`. |
| 4 | New lines 60-62 dropped the block's semicolon-terminator style and `{t, res}` spacing used elsewhere in the same template (lines 43, 51, and the pre-fix one-liner). Cosmetic only (ASI-safe). | 2 | — | **Fixed** — restored semicolons and `{t, res}` spacing. Commit `fff9340`. |

All other dimensions checked clean: AC-01, AC-03, AC-04, AC-05 (grep
independently re-run — `workflow-exec.md` is still the only `pipeline(`
call site in `skills/**/*.md`), US-01/US-02 coverage, §3 non-goals
(no skip-cascade implementation, no `pipeline()`/`parallel()` contract
rework, no test harness, no upstream proposal, no other-file changes), and
tasks.json/tracker AC-trailer mapping.

## Verdict

**PASS** (after fixes in commit `fff9340`). No open stage-1 finding remains.
